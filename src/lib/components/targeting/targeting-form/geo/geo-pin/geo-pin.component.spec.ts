/* tslint:disable:no-unused-variable */
import { GeoPinComponent } from './geo-pin.component';

describe('Component: GeoPin', () => {
  it('should create an instance', () => {
    let component = new GeoPinComponent();
    expect(component)
      .toBeTruthy();
  });
});
